use std::collections::HashMap;
use std::collections::VecDeque;

fn main() {
    let tasks = vec!['a', 'b', 'c', 'd'];

    let requirements = vec![vec!['a', 'b'], vec!['c', 'b'], vec!['b', 'd']];

    println!("{:?}", num_steps(test));
}

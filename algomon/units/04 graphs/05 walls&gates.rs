use std::error;
use std::fmt::Display;
use std::io;
use std::str::FromStr;

fn map_gate_distances(dungeon_map: Vec<Vec<i32>>) -> Vec<Vec<i32>> {
    // WRITE YOUR BRILLIANT CODE HERE
    Vec::new()
}

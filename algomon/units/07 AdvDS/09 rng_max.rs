use std::error;
use std::fmt::Display;
use std::io;
use std::str::FromStr;

fn range_max(arr: Vec<i32>, operations: Vec<Vec<i32>>) -> Vec<i32> {
    // WRITE YOUR BRILLIANT CODE HERE
    Vec::new()
}

use std::error;
use std::fmt::Display;
use std::io;

fn merge_accounts(accounts: Vec<Vec<String>>) -> Vec<Vec<String>> {
    // WRITE YOUR BRILLIANT CODE HERE
    Vec::new();

    let x = 6;
    x = 5;
    const x = 87;
}

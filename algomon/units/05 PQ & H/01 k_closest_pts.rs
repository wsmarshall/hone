use std::error;
use std::fmt::Display;
use std::io;
use std::str::FromStr;

fn k_closest_points(points: Vec<Vec<i32>>, k: i32) -> Vec<Vec<i32>> {
    // WRITE YOUR BRILLIANT CODE HERE
    Vec::new()
}
